# API差異分析レポート

モックアップから策定したAPI仕様と現行実装の差異分析

---

## 1. 認証 (Authentication)

| 仕様 | 実装状況 | 差異 |
|-----|---------|-----|
| `GET /api/v1/auth/login` | ❌ 未実装 | Azure AD認証はフロントエンドで直接処理 |
| `GET /api/v1/auth/callback` | ❌ 未実装 | 同上 |
| `POST /api/v1/auth/logout` | ❌ 未実装 | フロントエンドでトークン破棄 |
| `GET /api/v1/auth/me` | ✅ 実装済み | `/api/v1/user_account/me` として実装 |

**備考**: 認証フローはAzure AD SDKで処理されるため、バックエンドでの実装は不要

---

## 2. ダッシュボード (Dashboard)

| 仕様 | 実装状況 | 差異 |
|-----|---------|-----|
| `GET /api/v1/dashboard/stats` | ❌ **未実装** | 統計情報の集計APIが必要 |
| `GET /api/v1/dashboard/activities` | ❌ **未実装** | アクティビティログAPIが必要 |
| `GET /api/v1/dashboard/charts` | ❌ **未実装** | チャート用データAPIが必要 |

**新規開発が必要**

---

## 3. プロジェクト (Projects)

| 仕様 | 実装状況 | 差異 |
|-----|---------|-----|
| `GET /api/v1/projects` | ✅ 実装済み | `/api/v1/project` |
| `POST /api/v1/projects` | ✅ 実装済み | `/api/v1/project` |
| `GET /api/v1/projects/{id}` | ✅ 実装済み | `/api/v1/project/{project_id}` |
| `PUT /api/v1/projects/{id}` | ✅ 実装済み | `PATCH /api/v1/project/{project_id}` |
| `DELETE /api/v1/projects/{id}` | ✅ 実装済み | `/api/v1/project/{project_id}` |
| `PATCH /api/v1/projects/{id}/archive` | ⚠️ 部分実装 | `is_active`フラグで代用可能だが専用エンドポイントなし |

**差異詳細**:

- パスが複数形 `projects` vs 単数形 `project`
- 仕様の`start_date`, `end_date`フィールドは未実装
- 仕様の`stats`（統計情報）は未実装

---

## 4. プロジェクトメンバー (Project Members)

| 仕様 | 実装状況 | 差異 |
|-----|---------|-----|
| `GET /api/v1/projects/{project_id}/members` | ✅ 実装済み | `/api/v1/project/{project_id}/member` |
| `POST /api/v1/projects/{project_id}/members` | ✅ 実装済み | `/api/v1/project/{project_id}/member` |
| `PUT /api/v1/projects/{project_id}/members/{user_id}` | ✅ 実装済み | `PATCH`メソッドで実装 |
| `DELETE /api/v1/projects/{project_id}/members/{user_id}` | ✅ 実装済み | |

**差異詳細**:

- パスが `members` vs `member`
- 実装には`/bulk`一括追加、`/me`自分情報取得がある（仕様にない追加機能）

---

## 5. ファイル管理 (Files)

| 仕様 | 実装状況 | 差異 |
|-----|---------|-----|
| `GET /api/v1/projects/{project_id}/files` | ✅ 実装済み | `/api/v1/project/{project_id}/file` |
| `POST /api/v1/projects/{project_id}/files` | ✅ 実装済み | |
| `GET /api/v1/projects/{project_id}/files/{id}` | ✅ 実装済み | |
| `GET /api/v1/projects/{project_id}/files/{id}/download` | ✅ 実装済み | |
| `DELETE /api/v1/projects/{project_id}/files/{id}` | ✅ 実装済み | |
| `GET /api/v1/projects/{project_id}/files/{id}/sheets` | ⚠️ 別パス | ドライバーツリー用に実装 |
| `GET /api/v1/projects/{project_id}/files/{id}/sheets/{sheet}/columns` | ⚠️ 別パス | ドライバーツリー用に実装 |

**差異詳細**:

- シート/列情報はドライバーツリー配下 (`/driver-tree/file/...`) で実装
- 分析セッション用には別途実装が必要
- `usage`（使用状況）フィールドは未実装

---

## 6. 分析セッション (Analysis Sessions)

| 仕様 | 実装状況 | 差異 |
|-----|---------|-----|
| `GET /api/v1/projects/{project_id}/sessions` | ✅ 実装済み | `/api/v1/project/{project_id}/analysis/session` |
| `POST /api/v1/projects/{project_id}/sessions` | ✅ 実装済み | |
| `GET /api/v1/sessions/{id}` | ⚠️ 別パス | プロジェクトIDが必要なパス |
| `PUT /api/v1/sessions/{id}` | ✅ 実装済み | |
| `DELETE /api/v1/sessions/{id}` | ✅ 実装済み | |
| `POST /api/v1/sessions/{id}/duplicate` | ❌ **未実装** | 複製機能なし |

**差異詳細**:

- 仕様の`category_id`, `template_id`は`issue_id`として統合実装
- 仕様の`axis_settings`は`AnalysisFileCreate`で設定
- 複製機能が未実装

---

## 7. 分析チャット (Analysis Chat)

| 仕様 | 実装状況 | 差異 |
|-----|---------|-----|
| `GET /api/v1/sessions/{session_id}/messages` | ❌ **未実装** | メッセージ履歴取得なし |
| `POST /api/v1/sessions/{session_id}/messages` | ⚠️ 部分実装 | `/chat`として実装、ただし「準備中」 |
| `GET /api/v1/sessions/{session_id}/steps` | ⚠️ 別構造 | ステップはCRUD形式で実装 |

**差異詳細**:

- メッセージ履歴の永続化・取得機能が未実装
- SSE（Server-Sent Events）対応が未実装

---

## 8. スナップショット (Snapshots)

| 仕様 | 実装状況 | 差異 |
|-----|---------|-----|
| `GET /api/v1/sessions/{session_id}/snapshots` | ⚠️ 部分実装 | セッション詳細に`snapshot_list`含む |
| `POST /api/v1/sessions/{session_id}/snapshots` | ❌ **未実装** | 手動保存機能なし |
| `GET /api/v1/snapshots/{id}` | ❌ **未実装** | 個別取得なし |
| `POST /api/v1/snapshots/{id}/restore` | ❌ **未実装** | 復元機能なし |

**新規開発が必要**

---

## 9. ドライバーツリー (Driver Trees)

| 仕様 | 実装状況 | 差異 |
|-----|---------|-----|
| `GET /api/v1/projects/{project_id}/trees` | ✅ 実装済み | `/driver-tree/tree` |
| `POST /api/v1/projects/{project_id}/trees` | ✅ 実装済み | |
| `GET /api/v1/trees/{id}` | ⚠️ 別パス | プロジェクトIDが必要 |
| `PUT /api/v1/trees/{id}` | ❌ 未実装 | 更新はノード単位 |
| `DELETE /api/v1/trees/{id}` | ✅ 実装済み | |
| `POST /api/v1/trees/{id}/duplicate` | ❌ **未実装** | 複製機能なし |
| `POST /api/v1/trees/{id}/calculate` | ✅ 実装済み | `/data` として実装 |

**差異詳細**:

- ツリー全体の更新APIがない（ノード単位で更新）
- `category_id`フィールドは未実装
- 複製機能が未実装

---

## 10. ツリーノード (Tree Nodes)

| 仕様 | 実装状況 | 差異 |
|-----|---------|-----|
| `POST /api/v1/trees/{tree_id}/nodes` | ✅ 実装済み | |
| `PUT /api/v1/trees/{tree_id}/nodes/{id}` | ✅ 実装済み | `PATCH`メソッドで実装 |
| `DELETE /api/v1/trees/{tree_id}/nodes/{id}` | ✅ 実装済み | |

---

## 11. 施策 (Policies)

| 仕様 | 実装状況 | 差異 |
|-----|---------|-----|
| `GET /api/v1/trees/{tree_id}/policies` | ⚠️ 別構造 | ノード単位で取得 |
| `POST /api/v1/trees/{tree_id}/policies` | ⚠️ 別構造 | ノード単位で作成 |
| `PUT /api/v1/policies/{id}` | ✅ 実装済み | `PATCH`メソッドで実装 |
| `DELETE /api/v1/policies/{id}` | ✅ 実装済み | |

**差異詳細**:

- 仕様はツリー単位、実装はノード単位
- 仕様の`description`, `cost`, `duration_months`, `status`フィールドは未実装
- 実装は`name`と`value`のみ

---

## 12. データ紐付け (Data Binding)

| 仕様 | 実装状況 | 差異 |
|-----|---------|-----|
| `GET /api/v1/trees/{tree_id}/bindings` | ⚠️ 別構造 | `/sheet` APIで代用 |
| `PUT /api/v1/trees/{tree_id}/bindings` | ⚠️ 別構造 | カラム設定APIで代用 |
| `POST /api/v1/trees/{tree_id}/bindings/refresh` | ❌ **未実装** | データ更新機能なし |

---

## 13. 計算結果 (Calculation Results)

| 仕様 | 実装状況 | 差異 |
|-----|---------|-----|
| `GET /api/v1/trees/{tree_id}/results` | ✅ 実装済み | `/data` として実装 |
| `POST /api/v1/trees/{tree_id}/results/export` | ✅ 実装済み | `GET /output` として実装 |

**差異詳細**:

- 仕様の`summary`, `policy_effects`は未実装
- 施策適用後のシミュレーション結果が未実装

---

## 14-16. 管理機能 (Admin)

| 仕様カテゴリ | 実装状況 |
|------------|---------|
| カテゴリマスタ | ❌ **未実装** |
| 検証マスタ | ❌ **未実装** |
| 課題マスタ | ⚠️ 読取専用 | `/analysis/template` として読取のみ |

**差異詳細**:

- すべての管理CRUD機能が未実装
- データはシードスクリプトで投入されている模様

---

## 17. ユーザー管理 (Users - Admin)

| 仕様 | 実装状況 | 差異 |
|-----|---------|-----|
| `GET /api/v1/admin/users` | ✅ 実装済み | `/api/v1/user_account` |
| `GET /api/v1/admin/users/{id}` | ✅ 実装済み | |
| `PUT /api/v1/admin/users/{id}` | ⚠️ 部分実装 | `PATCH /me` のみ |
| `PATCH /api/v1/admin/users/{id}/activate` | ❌ **未実装** | 有効化/無効化なし |

**差異詳細**:

- ユーザー詳細の`stats`, `projects`, `activities`は未実装
- ロール変更機能は未実装

---

## 18-19. その他

| 仕様 | 実装状況 |
|-----|---------|
| ロール一覧 | ❌ 未実装（コード内で定義） |
| ツリーテンプレート | ⚠️ 部分実装 | `/category`, `/formula` で代用 |

---

## サマリー

### 🔴 新規開発が必要（未実装）

| # | 機能 | 優先度 |
|---|-----|-------|
| 1 | ダッシュボード統計API | 高 |
| 2 | アクティビティログAPI | 高 |
| 3 | スナップショット手動保存・復元 | 高 |
| 4 | セッション複製機能 | 中 |
| 5 | ツリー複製機能 | 中 |
| 6 | 管理機能（カテゴリ/検証/課題マスタCRUD） | 中 |
| 7 | ユーザー有効化/無効化 | 中 |
| 8 | チャットメッセージ履歴取得 | 高 |
| 9 | データ紐付け更新機能 | 中 |

### 🟡 拡張が必要（部分実装）

| # | 機能 | 内容 |
|---|-----|-----|
| 1 | プロジェクト | `start_date`, `end_date`, `stats` 追加 |
| 2 | ファイル | `usage`（使用状況）追加 |
| 3 | 施策 | `description`, `cost`, `duration_months`, `status` 追加 |
| 4 | 計算結果 | `summary`, `policy_effects` 追加 |
| 5 | ユーザー詳細 | `stats`, `projects`, `activities` 追加 |

### 🟢 実装済み（パス/メソッド差異のみ）

- プロジェクト基本CRUD
- プロジェクトメンバー管理
- ファイル基本管理
- 分析セッション基本CRUD
- ドライバーツリー基本CRUD
- ノード管理
- 基本的な計算・出力

---

## パス命名規則の差異

| 仕様 | 現行実装 |
|-----|---------|
| `/projects` (複数形) | `/project` (単数形) |
| `/members` (複数形) | `/member` (単数形) |
| `/files` (複数形) | `/file` (単数形) |
| `/sessions` | `/analysis/session` |
| `/trees` | `/driver-tree/tree` |
| `/admin/users` | `/user_account` |

---

## 推奨アクション

### Phase 1: 高優先度（MVP必須）

1. ダッシュボードAPI実装
2. チャットメッセージ履歴永続化
3. スナップショット手動保存・復元

### Phase 2: 中優先度（機能拡充）

1. 複製機能（セッション/ツリー）
2. 施策フィールド拡張
3. 管理画面CRUD機能

### Phase 3: 低優先度（品質向上）

1. パス命名規則の統一
2. ユーザー詳細情報拡張
3. ロール一覧API
